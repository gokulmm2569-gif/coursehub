# CourseHub frontend

The current premium React experience is served by the Next.js app at the repository root. This directory is reserved as the standalone React client boundary described in the approved architecture; migration into a separate Vite client is deferred until the API slice is ready.

The frontend will consume the FastAPI service through `/api/v1` and use the royal-blue and restrained-gold design tokens already established in the root app.
